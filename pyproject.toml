[project]
name = "signalcore-runtime"
version = "0.1.0"
description = "Local-first verified runtime control plane for AI coding agents"
requires-python = ">=3.11"
license = {text = "MIT"}

[project.scripts]
signalcore = "signalcore_runtime.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
