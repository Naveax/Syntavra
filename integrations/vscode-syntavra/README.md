# Syntavra VS Code Extension

Provides a live savings badge, instant optimization modes, local dashboard launch, and save-triggered incremental repository indexing. It shells out only to the local `syntavra` executable and sends no source code to an extension service.
