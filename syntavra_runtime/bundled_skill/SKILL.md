---
name: syntavra
version: "0.0.1"
description: >
  Use the installed Syntavra pre-release runtime for structural navigation,
  host enforcement, secure execution, reversible compression, unbounded external
  session history with a bounded active window, output governance and fail-closed benchmarking.
compatibility: "Codex, Claude Code, Gemini CLI, OpenCode, Cursor, Windsurf, Copilot, Cline, Roo Code, Continue, Qwen Code, Antigravity and MCP clients."
metadata:
  author: Naveax
  distribution: bundled-runtime
  status: pre-release
  version_locked: true
---

# Syntavra bundled skill

Run `syntavra doctor` before relying on runtime enforcement. Use structural inspection before broad reads, route long processes through the zero-poll broker, keep raw output in exact evidence, preserve exact recovery handles during compression, keep the active context bounded, and never publish performance claims without a valid external receipt.
