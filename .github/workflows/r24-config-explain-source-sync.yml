name: Apply R24 Config Explain Source Sync

on:
  push:
    branches:
      - feature/full-parity-r24-config-explain-cli
    paths:
      - tools/apply_r24_config_explain_patch.py

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/full-parity-r24-config-explain-cli
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'
      - uses: dtolnay/rust-toolchain@1.82.0
        with:
          components: rustfmt
      - name: Apply assertion-locked source patch
        run: python tools/apply_r24_config_explain_patch.py
      - name: Format Rust sources
        run: cargo fmt --all
      - name: Commit synchronized sources
        run: |
          if git diff --quiet; then
            echo 'R24 config explain sources already synchronized'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            .github/workflows/full-engine-parity.yml \
            contracts/engine/descriptor.txt \
            crates/syntavra-cli/src/config_contract.rs \
            crates/syntavra-cli/src/main.rs \
            crates/syntavra-contracts/src/lib.rs \
            syntavra_runtime/engine_selector.py \
            tests/runtime/test_engine_selector_r4.py \
            tests/runtime/test_full_parity_catalog_v1.py \
            tools/run_engine_parity.py \
            tools/verify_full_parity_catalog.py
          git commit -m 'Apply native R24 config explain source sync'
          git push origin HEAD:feature/full-parity-r24-config-explain-cli
