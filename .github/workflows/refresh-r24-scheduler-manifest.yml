name: Refresh R24 Scheduler Branch Manifest

on:
  push:
    branches:
      - feature/full-parity-r24-scheduler-read-only-cli

permissions:
  contents: write

jobs:
  refresh:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/full-parity-r24-scheduler-read-only-cli
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Refresh canonical manifest
        run: python -m tools.refresh_manifest
      - name: Remove temporary workflow
        run: git rm .github/workflows/refresh-r24-scheduler-manifest.yml
      - name: Commit canonical manifest
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add MANIFEST.sha256
          git commit -m 'Refresh R24 scheduler integrity manifest'
          git push origin HEAD:feature/full-parity-r24-scheduler-read-only-cli
