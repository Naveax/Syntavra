name: Apply R25-R37 Rust Fixes

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'feature/full-parity-r25-r37-completion'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/full-parity-r25-r37-completion
      - uses: dtolnay/rust-toolchain@1.82.0
        with:
          toolchain: 1.82.0
          components: rustfmt
      - name: Apply fixes and format workspace
        shell: bash
        run: |
          git apply .patches/r25-r37-known-fixes.patch
          cargo fmt --all
          rm -f .patches/r25-r37-known-fixes.patch
          rmdir .patches 2>/dev/null || true
          rm -f .github/workflows/apply-r25-r37-rust-fixes.yml
      - name: Commit formatted fixes
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "R25-R37: fix and format native parity runtime" || exit 0
          git push origin HEAD:feature/full-parity-r25-r37-completion
