# Syntavra Gemini extension context

The repository bundles the `syntavra` Agent Skill under `skills/syntavra/`.
Activate it only for complex coding-agent tasks where repository context, exact evidence, long-session state, large outputs, tool overload, verification, token usage, latency, or cost materially matter. Skip trivial requests.

Use the canonical `SKILL.md`; do not duplicate its full instructions in the always-on context.
