---
name: signal-core
version: "0.3.0"
description: >
  Use the installed SignalCore runtime for structural navigation, host enforcement,
  secure execution, reversible compression, long-session continuity, output
  governance and fail-closed benchmarking.
compatibility: "Codex, Claude Code, Gemini CLI, OpenCode, Cursor, Windsurf, Copilot, Cline, Roo Code, Continue, Qwen Code, Antigravity and MCP clients."
metadata:
  author: Naveax
  distribution: bundled-runtime
---

# SignalCore bundled skill

Run `signalcore doctor` before relying on runtime enforcement. Use structural inspection before broad reads, route long processes through the zero-poll broker, keep raw output in exact evidence, use the sandbox for untrusted commands, preserve exact recovery handles during compression, and never publish performance claims without a valid SignalBench receipt.
