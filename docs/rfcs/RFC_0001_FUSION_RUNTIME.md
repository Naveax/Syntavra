# RFC 0001: Fusion Runtime

Adopt an executable local runtime rather than prompt-only recommendations. The runtime owns durable jobs, output boundaries, telemetry, structural evidence, memory, context actions, verifier identities and claim receipts. The public efficiency claim remains fail-closed until live paired evidence exists.
