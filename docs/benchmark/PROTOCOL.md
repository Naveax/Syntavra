# Paired benchmark protocol

The same model, reasoning, prompt, repository tree, verifier, permissions, timeout and cache condition must be used for both arms. Failed or unequal-work runs receive no efficiency credit. Required verifiers may not be skipped. Actual quota telemetry is mandatory for a quota-based 5X claim. Difficulty qualification alone is not performance proof.
