# Current results

## SignalCore v0.3 internal component evidence

`benchmarks/results/runtime-v03/internal.json` records a deterministic internal smoke benchmark:

- 10 language fixtures with symbol and call extraction in the committed corpus;
- transitive impact and token-budgeted repository-map generation;
- byte-exact reversible compression and chunk restoration;
- exact 256-event recovery through one summary-DAG root;
- installer idempotency and rollback;
- explicit degraded sandbox guarantees;
- bounded output governance;
- SignalBench manifest validation.

The artifact is not an external competitor run and its claim remains `5X_NOT_PROVEN`.
