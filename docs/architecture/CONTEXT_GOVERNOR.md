# Context governor

Default actions begin at 50% utilization and culminate in mandatory controlled handoff at 88%. Stable-prefix content is serialized deterministically. Full logs, old successful output and duplicate evidence are externalized before summaries or session splitting.
