# Evidence and memory

Exact evidence is content-addressed and verified on read. Persistent memory is scoped by project and user, uses SQLite WAL and FTS5 when available, records provenance, deduplicates identical observations and excludes superseded facts by default.
